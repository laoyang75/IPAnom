# Smoke Test 报告（小范围代码验证）

主版本：`Y_IP_Codex_RB2_5/重构2.md`

## 1) 目的

- 在跑“32×64 shard 全量”前，先做单 shard 小范围验证，确认 RB20_01→08→99 SQL 可执行、关键断言可通过、并发现需要修复的脚本问题。

## 2) run_id / 范围

- `run_id`：`rb20v2_20260107_183400_sg_990`
- `contract_version`：`contract_v1`
- smoke shard：`shard_id=0`
- `shard_plan`（手动，小范围）：`ip_long_start=1909889936`，`ip_long_end=1911889936`
  - 说明：本次 smoke 为“代码与口径执行验证”，未跑全量 ShardPlan SQL-only（全量需按 Gate-2 Runbook 跑）。

## 3) 执行链路

按顺序执行并成功落表：
- RB20_01A → RB20_01 → RB20_02 → RB20_03 → RB20_11 → RB20_04 → RB20_04P → RB20_05 → RB20_06 → RB20_07 → RB20_08 → RB20_99

## 4) 核心数与分布（验收证据）

来自 `rb20_v2_5.step_stats` / `rb20_v2_5.core_numbers`：

- 异常去重（全局，`shard_id=-1`）
  - `abnormal_dedup_rows = 78853`
- Source Members（shard 0）
  - `source_members_rows = 794293`
  - `source_members_abnormal_rows = 109`
  - `source_members_valid_rows = 794184`
- Natural Blocks（shard 0）
  - `natural_block_cnt_total = 242893`
  - `natural_block_cnt_ge4 = 56396`
  - `avg_member_cnt_total ≈ 3.27`
  - `member_cnt_total_p50/p90/p99/max = 2 / 5 / 12 / 8192`
- Pre Profile（shard 0）
  - `keep_block_cnt = 242873`，`drop_block_cnt = 20`（全部 `valid_cnt=0`）
  - `keep_member_cnt = 794273`，`drop_member_cnt = 20`
  - `device_density_valid_p50/p90/p99 = 1.75 / 3.142857... / 9`
  - `report_density_valid_p50/p90/p99 = 11.5 / 45.5 / 198.0933...`
- Step64（shard 0）
  - `window_rows_cnt = 8268`
  - `window_cnt0_rows_cnt = 1`（cnt=0 覆盖存在）
  - `split_events_cnt = 8268`
  - `split_events_cnt0_cnt = 1`
  - `cut_cnt = 260`
  - `final_block_cnt = 243133`
- Final Profile（shard 0）
  - `final_profile_block_cnt = 243133`
  - `final_profile_invalid_block_cnt = 0`
  - `final_mobile_device_ratio_valid_p50/p90 = 0 / 0`（分母=设备量，DP-010）
  - `final_worktime_report_ratio_valid_p50/p90 ≈ 0.324 / 0.764`（分母=上报量，DP-010）
  - `final_holiday_report_ratio_valid_p50/p90 ≈ 0.244 / 0.667`（假日=周末，DP-009）
- H（全局）
  - `h_block_cnt = 61`
  - `h_member_cnt = 93144`
- R1/E/F（shard 0）
  - `r1_member_cnt = 701129`
  - `e_atom_cnt_total/pass = 36121 / 34414`
  - `e_run_cnt_total = 421`，`e_short_run_cnt = 178`
  - `e_member_cnt = 696090`
  - `f_member_cnt = 5039`

## 5) QA_Assert（全部 PASS）

`SELECT assert_name, pass_flag, details FROM rb20_v2_5.qa_assert WHERE run_id='rb20v2_20260107_183400_sg_990' ORDER BY assert_name;`

结果：9 条 `severity=STOP` 全部 `pass_flag=true`（含守恒/互斥/无幽灵/Drop 映射不蒸发/Step64 cnt0 审计）。

## 6) 本次 smoke 发现并已修复的问题（已写入 CHANGELOG）

- RB20_01：CTE `sp` 作用域问题导致执行失败
- RB20_01A：全局 `step_stats` 不能写 `shard_id=NULL`（PK 不允许 NULL），改为 `-1`
- Views：`CREATE OR REPLACE VIEW` 不能重排/改名列，改为 `DROP+CREATE`
- 性能：RB20_04/07 两处大范围 join 优化，避免 DBHub 60s 超时
