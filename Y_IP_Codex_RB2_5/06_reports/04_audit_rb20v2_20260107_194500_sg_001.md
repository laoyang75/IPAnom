# RB20 v2.0 — 运行审计报告（不作为最终验收通过）

run_id：`rb20v2_20260107_194500_sg_001`  
schema：`rb20_v2_5`

## 1) 目的
对“项目结项简报”做数据库事实核验，判断该 run 是否满足唯一规范 `Y_IP_Codex_RB2_5/重构2.md` 的**可验收条件**。

## 2) 结论（Gate：不通过）
该 run 的 H/E/F 数量与 Keep 守恒在数值层面成立，但存在以下**硬性不合规**，因此**不能进入最终交付（07_release）**：

1. **per-shard 关键产物不全**：
   - `profile_final` 缺失 `shard_id=51,56`
   - `window_headtail_64/split_events_64/block_final/map_member_block_final` 在 `shard_id=51,56` 为空（但 `preh_blocks>0`）
2. **CIDR 链路关键实体缺失**：`map_member_block_natural` 在 `shard_id=1,3,5,9,11,20,27` 为 0（但这些 shard 的 `source_members>0`、`block_natural/profile_pre/preh_blocks` 存在），导致“成员→自然块映射”不可追溯，且 `drop_members` 映射存在蒸发。
3. **QA 断言未完整执行**：`qa_assert` 仅写入 2 条断言；按交付要求必须执行 `Y_IP_Codex_RB2_5/03_sql/RB20_99/99_qa_assert.sql`（包含 shard_cnt 与 per-shard 覆盖断言）形成完整 STOP 断言集。
4. **ShardPlan / shard_cnt 未被作为合同常量管控**：该 run 使用了 85 个 shard（0..84），但未看到“在执行前写死 shard_cnt 并全链路一致复用”的证据；且存在漏跑 shard 的事实，说明流程控制与 Gate 不足。

> 因为以上任一条都足以导致“漏跑 shard / 链路不可追溯 / 结果不可复现”，所以即使汇总数看起来“完美”，也不能验收通过。

---

## 3) 数据库证据（可复现 SQL + 当前结果）

### 3.1 H/E/F 与 Keep 守恒（数值层面成立）

SQL：
```sql
WITH p AS (SELECT 'rb20v2_20260107_194500_sg_001'::text AS run_id)
SELECT
  (SELECT count(*) FROM rb20_v2_5.source_members sm JOIN p ON sm.run_id=p.run_id) AS source_members_rows,
  (SELECT count(*) FROM rb20_v2_5.abnormal_dedup ab JOIN p ON ab.run_id=p.run_id) AS abnormal_dedup_rows,
  (SELECT count(*) FROM rb20_v2_5.keep_members km JOIN p ON km.run_id=p.run_id) AS keep_members_rows,
  (SELECT count(*) FROM rb20_v2_5.drop_members dm JOIN p ON dm.run_id=p.run_id) AS drop_members_rows,
  (SELECT count(*) FROM rb20_v2_5.h_members hm JOIN p ON hm.run_id=p.run_id) AS h_rows,
  (SELECT count(*) FROM rb20_v2_5.e_members em JOIN p ON em.run_id=p.run_id) AS e_rows,
  (SELECT count(*) FROM rb20_v2_5.f_members fm JOIN p ON fm.run_id=p.run_id) AS f_rows;
```

结果（已核验）：
- `source_members_rows=59,706,070`
- `abnormal_dedup_rows=78,853`
- `keep_members_rows=59,694,056`
- `drop_members_rows=11,999`
- `h_rows=12,594,080`
- `e_rows=44,983,330`
- `f_rows=2,116,646`
- `h+e+f = 59,694,056`（= keep）

### 3.2 ShardPlan 不合规（应为 64，实际为 85）
### 3.2 ShardPlan 实际为 85 shard（需作为 shard_cnt 合同常量前置声明）

SQL：
```sql
WITH params AS (SELECT 'rb20v2_20260107_194500_sg_001'::text AS run_id)
SELECT
  (SELECT count(*) FROM rb20_v2_5.shard_plan sp JOIN params p ON sp.run_id=p.run_id) AS shard_plan_rows,
  (SELECT count(DISTINCT shard_id) FROM rb20_v2_5.shard_plan sp JOIN params p ON sp.run_id=p.run_id) AS shard_plan_shard_cnt,
  (SELECT min(shard_id) FROM rb20_v2_5.shard_plan sp JOIN params p ON sp.run_id=p.run_id) AS shard_plan_min_shard,
  (SELECT max(shard_id) FROM rb20_v2_5.shard_plan sp JOIN params p ON sp.run_id=p.run_id) AS shard_plan_max_shard;
```

结果（已核验）：
- `shard_plan_shard_cnt=85`
- `min=0 max=84`

### 3.3 profile_final 缺失 shard（51、56）

SQL：
```sql
WITH p AS (SELECT 'rb20v2_20260107_194500_sg_001'::text AS run_id)
SELECT sp.shard_id
FROM rb20_v2_5.shard_plan sp
JOIN p ON sp.run_id=p.run_id
LEFT JOIN rb20_v2_5.profile_final pf
  ON pf.run_id=sp.run_id AND pf.shard_id=sp.shard_id
WHERE pf.shard_id IS NULL
ORDER BY sp.shard_id;
```

结果（已核验）：
- 缺失 shard：`51, 56`

进一步一致性检查（这两个 shard 在 PreH 已存在，但 Step64 与 Final 为空）：
```sql
WITH p AS (SELECT 'rb20v2_20260107_194500_sg_001'::text AS run_id),
shards AS (SELECT unnest(ARRAY[51,56]::int[])::int AS shard_id)
SELECT
  s.shard_id,
  (SELECT count(*) FROM rb20_v2_5.preh_blocks pb WHERE pb.run_id=p.run_id AND pb.shard_id=s.shard_id) AS preh_blocks,
  (SELECT count(*) FROM rb20_v2_5.window_headtail_64 w  WHERE w.run_id=p.run_id  AND w.shard_id=s.shard_id) AS window_headtail_64,
  (SELECT count(*) FROM rb20_v2_5.split_events_64  e  WHERE e.run_id=p.run_id  AND e.shard_id=s.shard_id) AS split_events_64,
  (SELECT count(*) FROM rb20_v2_5.block_final      bf WHERE bf.run_id=p.run_id AND bf.shard_id=s.shard_id) AS block_final,
  (SELECT count(*) FROM rb20_v2_5.profile_final    pf WHERE pf.run_id=p.run_id AND pf.shard_id=s.shard_id) AS profile_final
FROM shards s, p
ORDER BY s.shard_id;
```

结果（已核验）：
- shard 51：`preh_blocks=129,093`，但 `window_headtail_64=0 split_events_64=0 block_final=0 profile_final=0`
- shard 56：`preh_blocks=14,734`，但 `window_headtail_64=0 split_events_64=0 block_final=0 profile_final=0`

### 3.4 QA 断言未完整执行（只 2 条）

SQL：
```sql
SELECT assert_name, severity, pass_flag, details, created_at
FROM rb20_v2_5.qa_assert
WHERE run_id='rb20v2_20260107_194500_sg_001'
ORDER BY assert_name;
```

结果（已核验）：
- 仅 2 条：`conservation_keep_equals_hef`、`no_overlap_h_e`（均 PASS）

### 3.5 Drop 成员映射蒸发（DropMembers 必须存在 NaturalMap，但当前不满足）

SQL：
```sql
WITH p AS (SELECT 'rb20v2_20260107_194500_sg_001'::text AS run_id)
SELECT dm.shard_id, count(*) AS missing_cnt
FROM rb20_v2_5.drop_members dm
JOIN p ON dm.run_id=p.run_id
LEFT JOIN rb20_v2_5.map_member_block_natural map
  ON map.run_id=dm.run_id AND map.shard_id=dm.shard_id AND map.ip_long=dm.ip_long
WHERE map.ip_long IS NULL
GROUP BY dm.shard_id
ORDER BY missing_cnt DESC, dm.shard_id;
```

结果（已核验）：
- `missing_cnt>0`，集中在 `shard_id=20,9,5,11,3,1,27`
- 同时这些 shard 的 `map_member_block_natural` 行数为 0（说明 RB20_02 的“映射落表”链路未按合同落地）

---

## 4) 建议修复路径（推荐）

### 4.1 推荐：新 run_id 严格按 64 shard 重跑（进入 release 的唯一合规路径）
1. 生成新 `run_id`，按 `Y_IP_Codex_RB2_5/04_runbook/03_exec_agent_runbook_all_in_one.md` 执行。
2. `RB20_00D` 后强校验：`shard_cnt=<YOUR_SHARD_CNT>` 且 `min=0 max=<YOUR_SHARD_CNT-1>`，否则 STOP。
3. per-shard 4.1 校验必须 0 行返回（尤其 `profile_final missing shards`）。
4. 执行 `Y_IP_Codex_RB2_5/03_sql/RB20_99/99_qa_assert.sql`，确保 STOP 断言集完整且全部 PASS。
5. 生成 `07_release` 终验收包（包含 SQL 结果与关键分布）。

### 4.2 如果需要 shard_cnt>64
必须先走 `DP-014`，并把 `shard_cnt` 写入合同与 Runbook；同时 QA_Assert 必须包含 `shard_plan_matches_shard_cnt` 与 `per_shard_outputs_complete` 断言，否则仍不可验收。
