# 2026-03-13 服务器执行记录

## 1. 本地样本验证结论

主线样本 run：

- `rb20v2_20260313_234500_sg_910`

主线结果：

- `h_overlap_e = 0`
- `h_overlap_f = 0`
- `e_overlap_f = 0`
- `keep_cnt = hef_cnt = 559749`
- `valid0_h = 0`
- `h_lt4 = 0`
- `short_run_e = 0`
- `qa_fail = 0`

说明：

- 这表示“统一成员 -> 连续块 -> 切分 -> H/E/F 分流 -> QA”主线在 2 个重 shard 样本上已经跑通。
- 当前未继续收尾的是摘要层，尤其是 `build_e_cidr_summary.py` 的性能与稳定性。

## 2. 已确认并修补的问题

### 主线正确性

- `valid_cnt=0` 不再进入 H。
- H 不再接收 `<4 IP` 小块。
- `short_run` 不再进入 E。
- F 改为排除“实际 E 覆盖”，而不是排除所有 `is_e_atom=true`。
- `DP-016` 二次切分后的 `block_id_final` 命名冲突已修复。

### 主线性能/卡死风险

- `RB20_03 post_process`：
  - 原因：`profile_pre` / `map_member_block_natural` join 被 planner 退化成 nested loop。
  - 处理：改为块清单回填，并显式 `SET LOCAL enable_nestloop = off`。

- `RB20_11 step11_chunked`：
  - 原因：单 chunk 过大，单条 SQL 可跑到数分钟。
  - 处理：`BLOCK_CHUNK_SIZE` 改为可配置，样本验证使用 `100`；增加 chunk 日志、`statement_timeout`、失败冒泡。

- `RB20_04 / 04P / 06 / 07 / 08 / 99`：
  - 原因：大 join / anti-join 被 planner 退化成 nested loop。
  - 处理：统一加 `SET LOCAL enable_nestloop = off`、`work_mem`、`statement_timeout`。

### 服务器可执行性

- `orchestrate_fresh_start_v2.py`、`orchestrate_rb20_v2.py`、`run_cleanup.py`：
  - 已改为 UTF-8 读取 SQL 文件，避免 CentOS 7 / Python 3.6 默认 ASCII 解码失败。

- `orchestrate_fresh_start_v2.py`：
  - 已改为接受 `shard_plan` 实际生成数量。
  - 不再因为 `len(shards) != SHARD_CNT` 直接停止。
  - QA 使用实际 shard 数，而不是启动时写死的值。

## 3. 服务器同步目录

当前热修复目录：

- `/root/ip_hotfix_20260313/Y_IP_Codex_RB2_5`

说明：

- 服务器后台执行使用的是该目录中的 SQL 与 runbook。
- 不是原仓库路径，不会直接依赖本地机器目录。

## 4. 服务器端后台 run 记录

### 已失败的尝试

- `rb20v2_20260313_194850_sg_242_fix01`
  - 失败原因：`run_cleanup.py` 旧路径。

- `rb20v2_20260313_195047_sg_242_fix01`
  - 失败原因：ASCII 解码 SQL 文件失败。

- `rb20v2_20260313_195500_sg_242_fix02`
  - 失败原因：`shard_plan returned 243 shard_ids, expected 242`。

- `rb20v2_20260313_195700_sg_243_fix03`
  - 失败原因：`shard_plan returned 244 shard_ids, expected 243`。

### 当前运行中的后台任务

- `run_id = rb20v2_20260313_200300_sg_dynamic_fix04`
- `log = /root/ip_hotfix_20260313/rb20v2_20260313_200300_sg_dynamic_fix04.log`

最近确认状态：

- 已经过 `Phase 1`
- 已进入 `Phase 2`
- 日志显示：
  - `NOTICE: shard_plan returned 244 shard_ids; configured SHARD_CNT=243. Using actual shard count.`

说明：

- 这表示服务器端现在已经不再被 shard 数漂移阻断。
- 后续是否成功，需要继续观察该日志。

## 7. 2026-03-16 追加记录

### 7.1 异常确认

用户反馈：

- 服务器已跑 `2` 天以上仍未完成。

检查结论：

- 问题属实。
- 当时不是“完全不动”，而是卡在 `Phase 7 / RB20_06 / r1_members`。
- Python 主进程被停后，数据库里仍残留了多条 PostgreSQL backend 在继续跑 `INSERT INTO rb20_v2_5.r1_members ...`。
- 服务器负载一度达到：
  - `load average: 29.55, 30.73, 30.43`

### 7.2 已执行的处理

- 终止残留 Python orchestrator 进程
- 终止残留 PostgreSQL backend 查询
- 再次确认：
  - Python 侧已无旧 orchestrator
  - 数据库活跃查询只剩检查语句

### 7.3 Step06 修复

修复内容：

- `06_r1_members_shard.sql`
  - 移除无必要的 `source_members` 回连
  - `atom27_id` 改为直接由 `ip_long / 32` 计算
  - `h_members` 与 `map_member_block_final` 改为先物化再 join

- `orchestrate_fresh_start_v2.py`
  - `Phase 7` 并发单独配置

- 新增恢复脚本：
  - `Y_IP_Codex_RB2_5/04_runbook/orchestrate_phase7_resume.py`
  - 用于只续跑 `Phase 7 + QA`

### 7.4 复测结果

本地 2-shard 样本 run：

- `rb20v2_20260316_110500_sg_911`

结果：

- 主线通过
- `qa_fail = 0`
- `keep_cnt = hef_cnt`
- `06_r1_members_shard_0 = 3.38s`
- `06_r1_members_shard_1 = 1.94s`

### 7.5 当前服务器恢复任务

当前已不再整条重跑，而是只从 Phase 7 续跑：

- `run_id = rb20v2_20260313_200300_sg_dynamic_fix04`
- `resume_log = /root/ip_hotfix_20260313/rb20v2_20260313_200300_sg_dynamic_fix04_phase7_resume.log`

启动方式：

- `PHASE7_CONCURRENCY=4`

最近确认状态：

- 已启动 `orchestrate_phase7_resume.py`
- 日志已出现：
  - `Phase 7 shard summary: total=244, finished=0, pending=244`
  - `DONE: Shard 0: 06 R1 Members`
  - `DONE: Shard 16: 06 R1 Members`
  - `DONE: Shard 32: 06 R1 Members`
  - `DONE: Shard 48: 06 R1 Members`

负载观察：

- 恢复后服务器负载下降到：
  - `load average: 3.95, 15.55, 24.26`

说明：

- 相比之前的 `29+`，当前已经明显回落。
- 当前 Phase 7 恢复任务仍在运行中。

## 8. 2026-03-17 摘要层追加记录

以主线 run `rb20v2_20260313_200300_sg_dynamic_fix04` 为准：

- `h_blocks = 21365`
- `e_runs = 130048`
- `f_members = 2652717`

摘要层状态：

- `h_block_summary = 21365`
- `e_cidr_summary = 67682`
- `f_ip_summary = 2652717`

说明：

- H 摘要已经完成，数量与 `h_blocks` 对齐。
- E 摘要已经完成，当前行为是每个 shard 汇总到 `e_run` / CIDR 层，总行数 `67682`。
- F 摘要已经完成，数量与 `f_members` 对齐。
- 当前主线摘要层三张表都已完成。

补充：

- E 摘要后台任务日志：
  - `/root/ip_hotfix_20260313/rb20v2_20260313_200300_sg_dynamic_fix04_e_summary.log`
- E 摘要完成时间：
  - `2026-03-17 11:59:32`

## 9. 2026-03-17 补充检查：摘要字段完整性与 PG 参数

### 9.1 H 摘要字段完整性

补充检查发现：

- 对 `run_id = rb20v2_20260313_200300_sg_dynamic_fix04`
- `h_block_summary` 行数虽然已经达到 `21365`
- 但 `start_ip_text`、`avg_apps_per_ip` 等派生列仍存在未回填状态

示例检查结论：

- `start_ip_text_nulls = 21365`
- `avg_apps_per_ip_nulls = 21365`

说明：

- 这表示“H 摘要表已建出行数”不等于“H 摘要表已完全可用于 UI”
- 后续 UI 或数据库修复时，必须把“数量完成”和“关键展示字段完成”分开检查

对比：

- `e_cidr_summary` 同 run 下 `start_ip_text_nulls = 0`

后续处理：

- 已终止孤儿 PostgreSQL backend：
  - `pid = 37366`
  - 该 backend 持续执行 `UPDATE rb20_v2_5.h_block_summary ...`
  - 其调用方连接已断开，但 backend 仍在数据库中继续运行

- 已修复 `build_h_block_summary.py` 的派生列回填逻辑：
  - 不再按相关子查询反复回扫 `block_final`
  - 改为两步：
    - 先在 `h_block_summary` 内回填均值类字段
    - 再从 `h_members` 聚合 `MIN(ip_long)` 回填 `start_ip_text`

- MCP 验证结论：
  - 在当前大 run 上，`h_members` 聚合块起始 IP 的实际执行时间约 `880ms`
  - 事务模拟下，回填后：
    - `null_start_ip_after = 0`
    - `null_avg_apps_after = 0`

- 已正式执行回填并验证：
  - `start_ip_text_nulls = 0`
  - `avg_apps_per_ip_nulls = 0`
  - `avg_devices_per_ip_nulls = 0`
  - 当前已无残留活跃后台查询

- 已同步服务器热修复脚本：
  - `/root/ip_hotfix_20260313/Y_IP_Codex_RB2_5/04_runbook/build_h_block_summary.py`

## 10. 2026-03-17 H 低有效块局部修复

问题定义：

- 当前 run 中发现 H 仍包含 `valid_cnt < 4` 的块。
- 这批块虽然 `member_cnt_total >= 4`，但有效 IP 已经退化，不符合“核心连续网络对象”的业务定义。
- UI 中看到的“H 库 1 IP / 2 IP / 3 IP”现象，本质上就是这批块被保留进了 H。

根因：

- `05_h_blocks_and_members.sql` 当时仍允许：
  - `valid_cnt > 0`
  - `member_cnt_total >= 4`
- 这会让“总成员很多、有效 IP 很少”的块继续进 H。

本次局部修复范围：

- `bad_h_blocks = 119`
- `bad_h_member_cnt_total = 2911`
- `bad_h_valid_cnt_total = 278`
- 受影响 shard 共 `18` 个：
  - `5,6,27,39,72,74,86,94,119,125,128,193,217,218,219,220,226,239`
- 其中需要重算 E 的 shard 共 `13` 个：
  - `5,27,39,72,74,119,125,128,193,217,219,220,239`
- 仅需重算 F 的 shard 共 `5` 个：
  - `6,86,94,218,226`

执行动作：

- 删除当前 run 中 `valid_cnt < 4` 的 H 块与成员
- 只对受影响 shard 局部重跑：
  - `RB20_06`
  - `RB20_07`（仅 E 受影响 shard）
  - `RB20_08`
- 局部重建：
  - `e_cidr_summary`
  - `f_ip_summary`
- `h_block_summary` 仅删除被移出的 H 块摘要行，不做整表重建

结果：

- `h_blocks` 从 `21365` 变为 `21246`
- `h_block_summary` 从 `21365` 变为 `21246`
- 当前 run 下：
  - `h_blocks.valid_cnt < 4 = 0`
  - `h_blocks.member_cnt_total < 4 = 0`
  - `MIN(h_blocks.valid_cnt) = 4`
  - `h_block_summary.ip_count < 4 = 0`
- 摘要层完整性：
  - `h_summary_not_ready = 0`
  - `e_summary_not_ready = 0`
  - `f_summary_not_ready = 0`
- 摘要与成员量：
  - `e_members = 40778394`
  - `f_members = 2654153`
  - `e_cidr_summary = 67682`
  - `f_ip_summary = 2654153`

当前 QA 状态：

- `qa_assert` 已恢复到可用状态，当前表内共有 `9` 条断言，全部通过，`STOP fail = 0`
- 已确认通过的关键断言包括：
  - `no_overlap_h_e`
  - `no_overlap_h_f`
  - `no_overlap_e_f`
  - `drop_members_have_natural_map`
  - `h_excludes_invalid_tier`
  - `h_excludes_valid_lt4`
  - `f_excludes_e_coverage`
  - `shard_plan_matches_shard_cnt`
  - `split_events_include_cnt0`

说明：

- 这次尝试直接重跑全量 `99_qa_assert.sql` 时，`conservation_keep_equals_hef` 查询过慢，导致本地重写 `qa_assert` 时中断。
- 为避免 `qa_assert` 长时间为空，已优先补回当前最关键的边界断言。
- 后续如果需要恢复“完整 11 条 QA 断言表”，应单独优化：
  - `conservation_keep_equals_hef`
  - `per_shard_outputs_complete`

### 9.2 PostgreSQL 在线参数调整

服务器环境：

- PostgreSQL `15.13`
- 内存约 `251G`
- CPU `40`
- `/data` 可用空间约 `4.3T`

结论：

- 当前问题不需要通过升级 PostgreSQL 主版本解决
- 更主要的问题是 SQL 形态、并发策略、临时文件观测与部分参数偏保守

已在线调整并确认生效：

- `track_io_timing = on`
- `wal_compression = on`
- `jit = off`
- `effective_cache_size = 200GB`
- `maintenance_work_mem = 16GB`
- `max_wal_size = 64GB`
- `min_wal_size = 8GB`
- `log_temp_files = 256MB`
- `temp_file_limit = 512GB`

未改动但当前保留：

- `shared_buffers = 64GB`
- `work_mem = 128MB`
- `max_parallel_workers_per_gather = 4`

保留原因：

- `shared_buffers = 64GB` 对当前机器已经是较高值，继续上调需要评估重启窗口与实际收益
- `work_mem` 已不低，若继续全局上调，容易在多并发 hash/sort 下放大总内存风险
- `max_parallel_workers_per_gather` 当前与主线分 shard 并发策略基本匹配，先不激进扩大

## 5. 暂未继续处理的部分

以下内容按用户要求暂缓：

- `build_e_cidr_summary.py` 的性能与分段化改造
- 摘要层全量服务器运行
- UI 调整

## 6. 下一步建议

优先顺序：

1. 持续观察服务器 run `rb20v2_20260313_200300_sg_dynamic_fix04`
2. 如果主线成功，再决定是否单独跑摘要层
3. 摘要层优先处理 `E summary`
4. 数据库部分稳定后再看 UI

### 9.3 2026-03-17 重型 QA 优化与局部回流修复

处理目标：

- 优化 `99_qa_assert.sql` 中两条重型 QA
- 复核并修复当前 run `rb20v2_20260313_200300_sg_dynamic_fix04` 的主线守恒问题

代码侧调整：

- `RB20_99 / conservation_keep_equals_hef`
  - 从“`keep_members` 对 `h/e/f` 三次大反连接”改为“分层计数守恒”
  - 新口径为：
    - `keep = h + r1`
    - `r1 = e + f`
    - `keep = h + e + f`
  - 当前 run 缺少 `RB20_03.keep_member_cnt` 时，先回退到直接 `COUNT(keep_members)`
- `RB20_03`
  - 新增 `keep_member_cnt` / `drop_member_cnt` 的 `step_stats`
- `RB20_06` / `RB20_07` / `RB20_08`
  - `SET LOCAL` 改为 `SET`，避免脚本独立执行时参数不生效
- `repair_h_valid_lt4_localized.py`
  - 新增 `keep -> r1 -> e/f` 的分 shard 强校验
  - 发现 gap shard 时，会对对应 shard 重新执行 `06/07/08`
- `run_subset_validation.py`
  - 主线校验从 `keep vs union(h/e/f)` 改成分层计数校验

本次额外发现的问题：

- 当前 run 虽然已经修掉了 `H(valid_cnt<4)`，但仍存在 `1455` 个 `keep_members` 没有进入 `H/E/F`
- 真实根因不在 UI，而在 Phase7 局部回流结果未完全闭合：
  - `e/f` 无交叉
  - 但有一批 IP 根本没有回到 `r1_members`
- 泄漏 shard 共 `10` 个：
  - `119,125,128,193,217,218,219,220,226,239`

数据库修复动作：

- 先验证单 shard：
  - 对 `119` 重跑 `RB20_06/07/08`
  - 漏掉的 `317` 个 IP 全部恢复
- 再对剩余 gap shard 顺序重跑 `RB20_06/07/08`
- 随后局部重建摘要：
  - `build_e_cidr_summary.py`
  - `rebuild_f_and_summary.py`
  - shard 范围：`119,125,128,193,217,218,219,220,226,239`

最终结果：

- `keep_members uncovered = 0`
- `h_blocks.valid_cnt < 4 = 0`
- `min(h_blocks.valid_cnt) = 4`
- `h_blocks = 21246`
- `h_block_summary = 21246`
- `e_runs = 130063`
- `e_cidr_summary = 67683`
- `f_members = 2655559`
- `f_ip_summary = 2655559`

最终 QA：

- `qa_assert = 12`
- `qa_pass = 12`
- `qa_stop_fail = 0`
- `conservation_keep_equals_hef = pass`
  - `keep_cnt=59694074`
  - `h_cnt=16260072`
  - `e_cnt=40778443`
  - `f_cnt=2655559`
  - `keep_minus_hef_gap=0`

性能记录：

- 新版 `99_qa_assert.sql` 本次完整执行耗时约 `517.68s`
- 本次重点优化的两条重型 QA 已可稳定完成，不再出现之前那种长时间卡在 `conservation_keep_equals_hef` / `per_shard_outputs_complete` 的状态
- 当前整套 QA 的主要耗时已转移到 `no_ghost_hef_outside_source`
